export interface IaboutData {
  domain: string;
  email: string;
  name: string;
}

export interface Ilogindata {
  displayName: string;
  email: string;
  expiresIn: string;
  idToken: string;
  kind: string;
  localId: string;
  refreshToken: string;
  registered: boolean;
}
